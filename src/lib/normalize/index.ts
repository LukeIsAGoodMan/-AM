// Normalization primitives for Stage 1A cross-check + reward normalization.
// numeric/percentage (§3B point 1), typed directional ratios (§4B), and the
// structured object-array comparison (§3B).
export * from "./numeric"
export * from "./ratio"
export * from "./structured-compare"
