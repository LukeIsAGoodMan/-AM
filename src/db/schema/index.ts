// Catalog tables (Layer 2) — populated starting M1
export * from "./catalog"

// User tables (Layer 7) — RESERVED, empty until catalog is stable
// export * from "./user"

// Extraction tables (Phase 2 — see PRD §22) — RESERVED, empty during MVP
// export * from "./extraction"
